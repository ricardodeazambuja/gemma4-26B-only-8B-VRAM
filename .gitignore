# Model weights (download with scripts/setup.sh — ~14 GB, do not commit)
models
models/*
!models/.gitkeep

# Locally built llama.cpp source + build (created by scripts/build-llama-cuda.sh)
vendor/

# Logs / scratch
*.log
/tmp/

# Python
__pycache__/
*.pyc

# Local working notes (kept out of the repo)
MEMORY.md

# Auto-tuning cache (machine-specific NCMOE/CTX results; written by start.sh / benchmark-config.sh)
.gemma4-tuning
